import { SelectByBorderDirective } from './select-by-border.directive';

describe('SelectByBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectByBorderDirective();
    expect(directive).toBeTruthy();
  });
});
